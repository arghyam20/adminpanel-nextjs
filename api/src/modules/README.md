# Backend Modules

Feature modules will live here when the separated Express API is introduced.

Recommended module shape:

```text
user/
  controller/
  routes/
  service/
  repository/
  validation/
  dto/
  types/
  interfaces/
  constants/
  swagger/
```
