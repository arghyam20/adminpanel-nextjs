CREATE DATABASE IF NOT EXISTS adminpanel_nextjs CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE adminpanel_nextjs;

-- Run `npm run prisma:migrate` to create the normalized schema from prisma/schema.prisma.
-- Seed credentials after `npm run prisma:seed`:
-- Email: admin@example.com
-- Password: Admin@12345
