# Backend Validations

Backend validation schemas will live here.

Rules:

- Backend validation must not import frontend validation.
- Validate every request boundary.
- Keep create, update, login, and query schemas separate when they differ.
